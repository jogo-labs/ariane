---
tagName: ar-dropdown
title: Dropdown
description: Panneau contextuel accessible, affiché au clic sur un bouton déclencheur et positionné automatiquement autour de lui.
playgroundTemplate: menu
variants:
    - name: menu
      label: Mode menu
      description: Avec des ar-dropdown-item, le panel reçoit automatiquement role="menu" et la navigation clavier est activée (↑↓ Home End Escape Tab).
      html: |
          <ar-dropdown>
              <button class="btn btn-secondary" slot="trigger">Actions ▾</button>
              <ar-dropdown-item><button>Modifier</button></ar-dropdown-item>
              <ar-dropdown-item><button>Dupliquer</button></ar-dropdown-item>
              <ar-dropdown-item><button>Supprimer</button></ar-dropdown-item>
          </ar-dropdown>
    - name: divider
      label: Séparateur
      description: >
          Un `<hr>` placé entre des `ar-dropdown-item` reçoit automatiquement `role="separator"`.
          Stylez-le avec votre propre CSS — ici via une variable CSS du token de bordure.
      html: |
          <ar-dropdown>
              <button class="btn btn-secondary" slot="trigger">Actions ▾</button>
              <ar-dropdown-item><button>Modifier</button></ar-dropdown-item>
              <ar-dropdown-item><button>Dupliquer</button></ar-dropdown-item>
              <hr style="border:none;border-top:1px solid var(--ar-color-border,#e5e7eb);margin:.25rem 0;">
              <ar-dropdown-item><button>Supprimer</button></ar-dropdown-item>
          </ar-dropdown>
    - name: free-content
      label: Contenu libre
      description: Sans ar-dropdown-item, le panel est un conteneur libre — aucun role ni navigation clavier n'est ajouté.
      html: |
          <ar-dropdown>
              <button class="btn btn-secondary" slot="trigger">Options ▾</button>
              <p style="padding: .5rem .75rem; margin: 0; color: var(--ar-color-text, inherit);">Contenu libre — formulaire, liste, texte…</p>
          </ar-dropdown>
    - name: placement-top
      label: Placement top-end
      description: Le panel s'ouvre au-dessus du trigger, aligné à droite.
      html: |
          <ar-dropdown placement="top-end">
              <button class="btn btn-secondary" slot="trigger">Placement ▾</button>
              <ar-dropdown-item><button>Item 1</button></ar-dropdown-item>
              <ar-dropdown-item><button>Item 2</button></ar-dropdown-item>
          </ar-dropdown>
    - name: distance
      label: Distance personnalisée
      description: 'Espacement en pixels entre le trigger et le panel (axe principal), piloté par la custom property CSS `--ar-dropdown-distance` (défaut : `var(--ar-anchor-distance)`, 4px).'
      html: |
          <ar-dropdown style="--ar-dropdown-distance: 16px">
              <button class="btn btn-secondary" slot="trigger">Distance 16px ▾</button>
              <ar-dropdown-item><button>Item 1</button></ar-dropdown-item>
              <ar-dropdown-item><button>Item 2</button></ar-dropdown-item>
              <ar-dropdown-item><button>Item 3</button></ar-dropdown-item>
          </ar-dropdown>
    - name: offset
      label: Offset latéral
      description: 'Décalage latéral en pixels du panel par rapport au trigger (axe transversal), piloté par la custom property CSS `--ar-dropdown-offset` (défaut : `var(--ar-anchor-offset)`, 0).'
      html: |
          <ar-dropdown style="--ar-dropdown-offset: 32px">
              <button class="btn btn-secondary" slot="trigger">Offset 32px ▾</button>
              <ar-dropdown-item><button>Item 1</button></ar-dropdown-item>
              <ar-dropdown-item><button>Item 2</button></ar-dropdown-item>
              <ar-dropdown-item><button>Item 3</button></ar-dropdown-item>
          </ar-dropdown>
    - name: external-trigger
      label: Trigger externe
      description: "L'attribut `for` accepte l'ID d'un élément déclencheur situé en dehors du composant. Utile pour les boutons dans une barre d'outils ou une autre zone de la page."
      html: |
          <button class="btn btn-secondary" id="ar-doc-ext-trigger">Trigger externe ▾</button>
          <ar-dropdown for="ar-doc-ext-trigger">
              <ar-dropdown-item><button>Modifier</button></ar-dropdown-item>
              <ar-dropdown-item><button>Dupliquer</button></ar-dropdown-item>
              <ar-dropdown-item><button>Supprimer</button></ar-dropdown-item>
          </ar-dropdown>
    - name: disabled
      label: Désactivé
      description: |
          Posez `disabled` sur `ar-dropdown` pour bloquer la logique JS, et `aria-disabled="true"`
          (pas l'attribut natif `disabled`) sur le trigger pour qu'il reste focusable — les technologies
          d'assistance annoncent l'état et peuvent lire la raison via `aria-describedby`.
          WCAG exempte les composants désactivés des exigences de contraste : `opacity: .5` est donc conforme.
      html: |
          <ar-dropdown disabled>
              <button
                  class="btn btn-secondary"
                  slot="trigger"
                  aria-disabled="true"
                  aria-describedby="disabled-reason"
                  style="opacity:.5;cursor:not-allowed;"
              >Actions ▾</button>
              <ar-dropdown-item><button>Item 1</button></ar-dropdown-item>
          </ar-dropdown>
          <p id="disabled-reason" style="margin-top:.5rem;font-size:.875rem;color:var(--ar-color-text-muted,#6b7280);">
              Droits insuffisants pour effectuer cette action.
          </p>
---

import WcagRef from '../../components/WcagRef.astro';

## Accessibilité

### Pris en charge automatiquement

- Le trigger reçoit `aria-haspopup="true"` et `aria-expanded` synchronisé à l'état ouvert/fermé —
    <WcagRef
        criterion="4.1.2"
        summary="Name, Role, Value : tout composant UI doit exposer son nom, rôle et valeur (états inclus) aux technologies d'assistance."
    />
- En mode menu (présence d'`ar-dropdown-item`), le panel reçoit `role="menu"` et chaque enfant focusable reçoit `role="menuitem"`.
- Navigation clavier complète : `↑` `↓` `Home` `End` pour déplacer le focus, `Escape` et `Tab` pour fermer —
    <WcagRef
        criterion="2.1.1"
        summary="Keyboard : toutes les fonctionnalités doivent être accessibles au clavier sans temporisation spécifique."
    />
- Retour du focus sur le trigger à la fermeture.
- Light-dismiss natif (`popover="auto"`) : un clic hors du panel ferme automatiquement.
- Les animations sont désactivées si `prefers-reduced-motion: reduce` est actif.

### À votre charge

- **Le bouton du trigger doit être accessible.** Fournir un libellé textuel clair ou un `aria-label` si le trigger ne contient que de l'iconographie.
- **En mode menu, envelopper chaque action dans `ar-dropdown-item`.** Les éléments sans `ar-dropdown-item` dans un panel menu ne reçoivent pas `role="menuitem"` et sortent de la navigation clavier.
- **Pour l'état désactivé, coupler `ar-dropdown[disabled]` et `aria-disabled="true"` sur le trigger.** L'attribut `disabled` du composant bloque la logique JS ; `aria-disabled="true"` (à la place du `disabled` natif) maintient le bouton dans l'ordre de tabulation pour que les technologies d'assistance puissent annoncer l'état et lire une éventuelle raison via `aria-describedby`.
